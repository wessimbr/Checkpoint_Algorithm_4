#Algorithm_4_Palindrome

The algorithm checks if a word is a palindrome by comparing characters at both ends 
and recursively checking the inner characters,
stopping when differences are found or when the word becomes empty or contains only one character.